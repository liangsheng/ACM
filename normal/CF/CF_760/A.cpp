#include <bits/stdc++.h>
using namespace std;
typedef long long LL;

const int N = 105;

int T, n;
LL a[N];

int main() {
    // ios::sync_with_stdio(false);
    // cin.tie(0);
    cin >> T;
    while (T--) {
        cin >> n;
        for (int i = 1; i <= n; i++) cin >> a[i];
    }
    return 0;
}